Please write a chess bruteforcer.
So that `make` yields output of a test that is chosen-n-constructed by you.
Even if you think your code is useless, I'll send you some LTC to address in `cat FEEDBACK`.
